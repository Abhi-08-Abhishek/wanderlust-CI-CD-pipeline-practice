# Backend Docker Environment
PORT=5000
NODE_ENV=production

# MongoDB container hostname (from docker-compose)
MONGODB_URI=mongodb://mongo:27017/wanderlust

# Redis container hostname
REDIS_URL=redis://redis:6379
